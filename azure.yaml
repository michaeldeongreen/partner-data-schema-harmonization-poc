# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: partner-data-schema-harmonization
metadata:
  template: partner-data-schema-harmonization-poc@0.0.1-beta

services:
  web:
    project: ./src/SchemaHarmonizer
    language: csharp
    host: appservice

infra:
  provider: bicep